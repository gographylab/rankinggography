Strip viewfinder to corner brackets only on Cover of the week

Remove the grid, center crosshair, and HUD text strips (REC, camera,
lens, iso, shutter, aperture, AF) from the landing's "Cover of the
week" viewfinder. Image now sits on the black background framed only
by the 4 L-shaped corner brackets.

ViewfinderFrame keeps full functionality — added a `showAF` prop to
gate the previously-hardcoded AF strip so future callers can choose
the bare-frame look (showGrid/showCrosshair/showAF all false +
no label props) or the full HUD.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
